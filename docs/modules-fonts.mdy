---
mm: 1.0
kind: doc
name: modules.cpp bitmap font module
status: official
release: v1.2.1
---

# modules.cpp bitmap font module

# Status and authority

status: official gives the implemented portions of this document the same
authority as the other v1.2.1 specifications. This document specifies the
implemented mm.fonts module: the Glyph, Font, and TextMetrics types, the
measure and render functions, and the committed kMono12 and
kMono16 glyph tables derived from IBM Plex Mono. The font-demo application
exists. Legible text on a physical panel remains a hardware-qualification
requirement rather than a completed claim.

docs/modules-display.mdy owns the portable display boundary and its pixel
packing. docs/modules-gfx.mdy owns surfaces, drawing, expansion, and rotation.
docs/modules-platform-mcu.mdy owns the MCU provider model. mm.fonts consumes
mm.gfx and mm.display and does not weaken either boundary for one panel.

# Role

mm.fonts composes a UTF-8 string into a packed one-bit frame. A one-bit
display writes those bytes directly; a sixteen-bit display needs RGB565
conversion before writing. mm.display moves packed pixels and names the
boundary in its own manifest: graphics policy, fonts, image decoding, and
controller commands live above or below it, not inside it. Text support closes
that gap for the two panels this project targets, the 152 by 296 one-bit
ePaper and the 240 by 320 sixteen-bit RGB565 LCD.

The module obeys the standing project rules:

- No vector parsing and no font engine on the target. The bitmaps are
  precomputed at development time and shipped as static data.
- No heap allocation in the font module at runtime. The committed Font
  instances hold spans over generated static glyph tables; Glyph and
  TextMetrics hold scalar values. The caller owns the Surface passed to
  render.
- One composition format for both panels. Text is composed into a one-bit
  frame; the one-bit panel receives those rows, and the sixteen-bit panel
  receives RGB565 rows converted by mm.gfx.
- Monospace by design. A fixed cell width makes wrapping and alignment
  arithmetic, which is what a terminal-style panel UI wants.

# IBM Plex Mono

The committed tables are derived from IBM Plex Mono, IBM's monospace
typeface, licensed under the SIL Open Font License 1.1 (OFL). The OFL is an
open source font licence, copyright 2017 IBM Corp., with the reserved font
name "Plex". Its terms that reach this project:

- It permits use, study, copy, merge, embed, modify, redistribution, and
  sale of modified and unmodified copies.
- The font software may not be sold by itself, which firmware never does.
- Every distributed copy carries the copyright notice and the licence. The
  repository carries the OFL text at modules/mm/fonts/licenses/OFL.txt
  beside the Apache 2.0 root licence, and each generated table names its
  provenance in a header comment.
- The reserved name "Plex" may not be the primary user-facing name of a
  derivative work. The module keeps "Plex" to provenance attribution and
  keeps its identifiers neutral, kMono12 and kMono16 rather than kPlex12.

The face facts the module relies on:

- Coverage: 1049 glyphs, unitsPerEm 1000, all 95 printable ASCII code points,
  and all 18 Polish letters, ą ć ę ł ń ó ś ź ż in both cases.
- Monospacing: every printable-ASCII advance is exactly 600 units, 0.600 em.
  The face is a true monospace, which is what a fixed-cell display UI wants.
- Format: vector outlines only. IBM ships no bitmap variant, which is why the
  module's bitmaps are generated from the TTF at development time instead of
  parsed at runtime.

# Module layout

Opening fence
modules/mm/fonts/
    mm.mdy
    fonts.cppm
    types.cppm
    renderer.cppm
    data.cppm
    src/renderer.cpp
    licenses/OFL.txt
Closing fence

mm.fonts is one module with three partitions. types carries the data shapes,
renderer carries the pure composition logic, and data carries the generated
tables. fonts re-exports the three, so a consumer imports only mm.fonts.

Opening fence
mm: 1.2
kind: module
name: fonts
module: mm.fonts
use: mm.display
use: mm.gfx
file: types.cppm mm.fonts:types
file: renderer.cppm mm.fonts:renderer
file: data.cppm mm.fonts:data
file: fonts.cppm mm.fonts
file: src/renderer.cpp
Closing fence

# Types

Opening fence
struct Glyph {
    unsigned int code_point = 0;   // sorted ascending in the index
    unsigned int x_offset = 0;     // ink left edge, pixels from the cell origin
    unsigned int width = 0;        // tight ink width in pixels
    unsigned int byte_offset = 0;  // offset into Font::data
    [[nodiscard]] constexpr unsigned int row_bytes() const;
};

struct Font {
    std::span<const std::byte> data;
    std::span<const Glyph> glyphs;
    unsigned int advance = 0;      // monospace cell width, pixels
    unsigned int height = 0;       // ascent + descent
    unsigned int ascent = 0;
    unsigned int line_height = 0;  // height plus the row gap the face wants
};

struct TextMetrics {
    unsigned int width = 0;
    unsigned int height = 0;
};
Closing fence

Font::data holds every glyph bitmap: per glyph, one bit per pixel, most
significant bit left, each row padded to a byte, rows top to bottom from the
ascent line. A glyph's rows begin at its byte_offset and are row_bytes()
wide. The index is sorted ascending by code point, is searched by binary
search, and leads with the space glyph at zero width.

# Renderer

Opening fence
[[nodiscard]] mm::display::Status render(
    const char8_t* text, std::size_t text_size, const Font& font,
    mm::display::Color ink, unsigned int x, unsigned int y,
    mm::gfx::Surface surface);

[[nodiscard]] TextMetrics measure(const char8_t* text,
                                  std::size_t text_size, const Font& font);

Closing fence

render composes UTF-8 text into a one-bit mm.gfx::Surface. Composition is
transparent: glyph pixels take the ink, and every other pixel is left
untouched. White ink sets glyph bits; black ink clears them. A caller that
wants a cleared region fills it before rendering. The surface width is the
visible right edge, and its height is the bottom boundary for text.

The host tests pin these semantics down:

- A code point absent from the font renders blank, and the pen still advances
  one cell for it.
- Text that reaches the right edge of the frame is clipped, not an error.
- render answers BadArgument for null text with a nonzero size, an invalid
  or non-one-bit surface, text reaching past its bottom, Red ink, a glyph
  table extending beyond its data,
  and malformed UTF-8. The host tests cover a truncated sequence. measure is
  lenient by design: a malformed sequence ends the count, and nothing is
  measured past it.
- A glyph row is copied as whole bytes and rotated at its two edges, so the
  common case needs no per-pixel loop.

The decoder also rejects overlong, surrogate, and out-of-range sequences;
those cases are not separately exercised by the current host tests.

measure is the monospace contract: width is the count of well-formed code
points times the advance, and height is the font's line height. No per-glyph
scanning, so it costs nothing.

expand_row maps one packed one-bit row to RGB565 for the sixteen-bit panels,
most significant byte first: bit 1 becomes the foreground, bit 0 the
background, two bytes per pixel. A 240-pixel row is 480 bytes. It answers
BadArgument for a packed row shorter than its declared width or an output row
shorter than the expanded one.

rotate turns a packed one-bit frame clockwise by quarter turns. An odd turn
swaps its width and height. The source and destination must not overlap
(a precondition, not a checked error); the destination padding bits are
cleared. It answers BadArgument for zero dimensions or a span too short
for the source or turned frame.

# Glyph data

data.cppm carries the two committed tables. Both are generated from the
pinned IBMPlexMono-Regular.ttf at development time, and both hold the same
113 code points: the 95 printable ASCII plus the 18 Polish letters.

- kMono12: 113 glyphs, 1904 packed bytes, advance 7, height 17, ascent 13,
  line_height 17.
- kMono16: 113 glyphs, 4928 packed bytes, advance 10, height 22, ascent 17,
  line_height 22.

data.cppm is a generated file and must not be edited by hand; regenerating it
from the pinned TTF is the only supported change. Its header carries the
provenance the licence requires: the face, the TTF's SHA256, the charset, and
each table's glyph count and packed size, with the OFL attribution beside
licenses/OFL.txt.

Both tables fit both boards' flash with room for the rest of the project. At
the committed advances:

- 12 px, advance 7, line_height 17: 21 columns by 17 lines on the 152 by 296
  ePaper, 34 by 18 on the 240 by 320 LCD.
- 16 px, advance 10, line_height 22: 15 columns by 13 lines on the ePaper,
  24 by 14 on the LCD.

A full 152 by 296 one-bit frame contains 296 rows of 19 bytes, or 5,624
bytes. A full 240 by 320 RGB565 frame contains 320 rows of 480 bytes, or
153,600 bytes. The LCD provider shows pixels as they are written, so its
refresh call sends nothing further. Font composition and RGB565 conversion
still take CPU time; this document makes no unmeasured timing claim.

# Both panels from one sequence

A caller draws the same one-bit text composition on either panel: compose,
convert rows when needed, write, refresh.

- Compose the lines into a one-bit frame a full panel row wide, centring each
  line with measure. Full rows satisfy the ePaper's write contract: x zero is
  a multiple of 8, and both panel widths, 152 and 240, are multiples of 8, so
  no row needs special handling.
- On a one-bit panel, mm.gfx::write sends the packed frame directly.
- On a sixteen-bit panel, mm.gfx::write expands each row through a base
  palette and optional rectangular palette regions before sending it.
- Refresh is always explicit and unchanged by this module.

apps/font-demo clears the panel white, composes four centred lines, two at
16 pixels and two at 12, then shows every inked glyph in the committed
charset. It turns the composed frame through all four quarter-turn
orientations, holds each view long enough to be read, and sleeps. On the LCD,
palette selection follows each pixel's original logical row after rotation,
represented by a rotated Region for each line. The demo uses mm.gfx to turn
the frame and regions and to write either panel depth. It names mm.fonts,
mm.gfx, mm.display, and mm.mcu, with no board, SDK, or vendor dependency.

# Test strategy

The module's host tests under tests/mm/fonts exercise the pure font logic
without a display provider:

- The committed kMono12 and kMono16 index and data match a small golden set
  of glyph bitmaps, so a corrupted regeneration fails the suite.
- render of a known glyph at a known origin equals the expected packed bytes,
  in both polarities.
- Rendering over a pre-marked frame leaves the marked bits untouched in both
  polarities, the transparent composition contract.
- Ink straddling a frame byte boundary lands in both bytes, in both
  polarities.
- A missing code point renders blank and the pen still advances.
- measure obeys the monospace contract and counts multi-byte Polish letters
  one cell each.
- Text reaching the right edge is clipped rather than rejected.
- BadArgument: invalid Surface, text past its last row, Red ink, truncated
  UTF-8, a glyph table beyond its data, and null text with a nonzero size.
  Expansion and rotation tests live under tests/mm/gfx.

# Implementation stages

1. Implemented: add mm.fonts:types and mm.fonts:renderer with host tests.
2. Implemented: generate and commit the kMono12 and kMono16 IBM Plex Mono
   tables with their provenance header and the OFL text.
3. Implemented: add apps/font-demo drawing both sizes, the Polish line, the
   full twelve pixel charset, and all four quarter-turn orientations, on
   both panel kinds.
4. Pending hardware: qualify legibility, polarity, alignment, and refresh on
   the named ePaper and LCD boards.

# Acceptance criteria

- The mm.fonts host tests pass without a display provider, Pico SDK, or
  hardware.
- No font table or renderer object appears in an executable that does not
  import mm.fonts.
- The same composed frame drives both panel kinds through mm.gfx and
  mm.display.
- Full-row composition satisfies the ePaper's byte alignment on both panel
  widths.
- font-demo draws both committed sizes, the Polish line, and the full
  charset in all four orientations on a named display board before
  legibility is claimed.
- A successful build is not reported as legible text on a panel.
- The committed tables carry their provenance header, and the OFL text sits
  beside them in the repository.

# What this does not settle

- Antialiasing and grayscale glyph masks are not implemented; the committed
  bitmaps have one-bit coverage. The LCD accepts RGB565 colours chosen when
  the one-bit frame is expanded, while the ePaper write path remains one-bit.
- No 8 px table is committed. Plex Mono at 8 px is soft, the limit of a modern
  display face at an eight-pixel cell. An 8 px table would be a data question,
  another generated Font constant, not a design change.
- Weights, italics, and proportional metrics are not in the committed face or
  format; the data partition holds any number of tables if a face is wanted.
- RLE compression of glyph rows is deferred: the storage saving does not
  justify a decoder on either board's flash budget.
- Charset growth beyond printable ASCII and the 18 Polish letters is a
  regeneration, not a format change.
- Kerning, ligatures, and text shaping are out of scope; the face is
  monospace and the charset is a flat code-point set.
- Drawing primitives, image formats, and UI layout remain outside mm.fonts.
- No change reaches mm.display, mm.epaper, or mm.lcd; this module consumes
  mm.gfx and mm.display.
