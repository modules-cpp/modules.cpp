---
mm: 1.0
kind: app
name: configure
use: mm.app
use: mm.build
use: mm.configure
file: configure.cpp
---

# configure tool

Writes `out/config.mdy` without running another command. `--compiler` accepts
gcc, g++, clang, or clang++, optionally followed by a numeric major version.
