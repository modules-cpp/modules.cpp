---
mm: 1.0
kind: module
name: model
module: mm.model
use: mm.build
use: mm.mdy
use: models.configuration
use: models.document
use: models.manifest
use: models.modules
use: models.repository
use: models.tool
use: models.workflow
file: model.cppm
file: src/model.cpp
---

# mm.model

Adapts mm.build's and mm.mdy's real data onto the abstract interfaces in models/, rather than duplicating either. Its use: list names every models module it maps onto, which is why it is the longest in the project.
